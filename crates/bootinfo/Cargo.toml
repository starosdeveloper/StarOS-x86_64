[package]
name = "staros-bootinfo"
description = "The loader → kernel hand-off contract on PC platforms"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
# The framebuffer the loader describes has to be *drawn on* by the kernel, and
# the two crates name the same byte orders differently — see PixelFormat's
# conversion, which exists so that inversion is written down once and tested.
staros-framebuffer.workspace = true
